EVALUATE
    SUMMARIZECOLUMNS(
        'Date'[date],
        'Service Group'[service_group_short],
        /* REPORT_FILTER */
        "Step Failures", [Step Failures]
    )
ORDER BY
    'Date'[date] ASC,
    'Service Group'[service_group_short] ASC
